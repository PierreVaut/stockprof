// import { server } from '../api/routes';

/* const chalk = require('chalk');
const io = require('socket.io')(server); */


export const emitNotification = (id, notification) => {
/*   io.emit(id, `GRUGRUGRUURUR ${notification}`);
  console.log(chalk.green(`[Emit ${id}]`, JSON.stringify(notification))); */
};

